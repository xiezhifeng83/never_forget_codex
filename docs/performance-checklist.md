# Performance Checklist

| Scenario | Target | Measured | Notes |
|----------|--------|----------|-------|
| SC-001 – Launch to add (initial run) | ≤ 3s | _TBD_ | Measure from app launch to task persisted |
| SC-008 – Status dropdown load | ≤ 1s | _TBD_ | Capture time from tray/notification click to dropdown render |
| SC-009 – Cold start list display | ≤ 1s | _TBD_ | Record time from app launch to first list paint |

> Update the **Measured** column after manual verification (see Quickstart manual verification section).
